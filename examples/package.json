{
  "name": "examples",
  "private": true,
  "description": "Consumer-level smoke checks for @prodkit/op installation paths.",
  "type": "module",
  "scripts": {
    "smoke": "node ./smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.12.2"
  },
  "engines": {
    "node": ">=24"
  }
}
