{
  "name": "@prodkit/op",
  "version": "0.1.53",
  "description": "A runtime-agnostic, composable library for writing predictable operations in TypeScript.",
  "keywords": [
    "composition",
    "effect",
    "error handling",
    "operations",
    "runtime-agnostic",
    "typescript"
  ],
  "homepage": "https://github.com/trvswgnr/op#readme",
  "bugs": {
    "url": "https://github.com/trvswgnr/op/issues"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/trvswgnr/op"
  },
  "files": [
    "dist",
    "CHANGELOG.md",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsdown --config-loader unrun",
    "changelog:check-version": "node ./scripts/check-changelog-version.ts",
    "check": "npm run typecheck && npm run lint && npm run fmt:check && npm run build && npm run test && npm run examples:smoke:pack && npm run examples:typecheck",
    "clean": "rm -rf dist",
    "examples:smoke:pack": "node ./scripts/run-examples-smoke.ts pack",
    "examples:smoke:github": "node ./scripts/run-examples-smoke.ts github",
    "examples:smoke:npm": "node ./scripts/run-examples-smoke.ts npm",
    "examples:smoke:all": "npm run examples:smoke:pack && npm run examples:smoke:github && npm run examples:smoke:npm",
    "examples:typecheck": "cd examples && npm run typecheck",
    "fix": "npm run lint:fix && npm run fmt",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "prepare": "npm run build",
    "prepublishOnly": "npm run check && npm run changelog:check-version",
    "release:prepare": "npm run check && npm run changelog:check-version && npm pack --dry-run",
    "release:cut:patch": "node ./scripts/release-cut.ts patch",
    "release:cut:minor": "node ./scripts/release-cut.ts minor",
    "release:cut:major": "node ./scripts/release-cut.ts major",
    "release:patch": "npm run release:cut:patch",
    "release:publish": "npm publish --access public --provenance",
    "release:push": "git push && git push --tags",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-result": "^2.9.0"
  },
  "devDependencies": {
    "oxfmt": "^0.40.0",
    "oxlint": "^1.55.0",
    "tsdown": "^0.21.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
