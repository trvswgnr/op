{
  "name": "@prodkit/op",
  "version": "0.1.3",
  "description": "Composable generator-based operations with typed results, typed errors, async suspension, and built-in retry semantics.",
  "keywords": [
    "composition",
    "effect",
    "error handling",
    "operations",
    "typescript"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/trvswgnr/op"
  },
  "bugs": {
    "url": "https://github.com/trvswgnr/op/issues"
  },
  "homepage": "https://github.com/trvswgnr/op#readme",
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsdown",
    "check": "npm run typecheck && npm run lint && npm run fmt:check && npm run build && npm run test",
    "clean": "rm -rf dist",
    "fix": "npm run lint:fix && npm run fmt",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "prepublishOnly": "npm run check",
    "release:prepare": "npm run check && npm pack --dry-run",
    "release:version": "npm version patch",
    "release:publish": "npm publish --access public --no-provenance",
    "test": "vitest run",
    "test:e2e": "vitest run **/*.e2e.test.ts",
    "test:integration": "vitest run **/*.integration.test.ts",
    "test:unit": "vitest run --exclude '**/*.e2e.test.ts' --exclude '**/*.integration.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.19.37",
    "oxfmt": "^0.40.0",
    "oxlint": "^1.55.0",
    "tsdown": "^0.21.2",
    "typescript": "^5.9.3",
    "valibot": "^1.3.1",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
